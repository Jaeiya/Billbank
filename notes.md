

## Go over the entire database
There are a lot of potentially new fields and order of fields
that need to be added/configured and tested for. Use the db
design doc to make sure everything matches.





## Think about a custom UI just for first-time database init
It would be nice if there was a built-in database creator that
popped up the first time a user started the application. This
could also be done inside the program UI itself, but ... I
need more time to marinate on this idea.
